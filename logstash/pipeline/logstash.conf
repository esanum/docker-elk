input {
  tcp {
      port => 5000
      codec => json_lines
  }
}

filter {
  if [fields][log_type] == "rails" {
    json {
      source => "message"
    }
  }
}

output {
  elasticsearch {
    hosts => "elasticsearch:9200"
    manage_template => false
    index => "logstashrails-%{+YYYY.MM.dd}"
    document_type => "log"
  }
}
